# PostgreSQL (default matches docker-compose.yml)
DATABASE_URL=jdbc:postgresql://localhost:5432/vibehunt
DATABASE_USER=vibehunt
DATABASE_PASSWORD=vibehunt

# Web client origins (comma-separated: React :8081, Compose :8082)
WEB_ORIGIN=http://localhost:8081,http://localhost:8082
FRONTEND_URL=http://localhost:8081

# Session cookie
SESSION_COOKIE_NAME=vibehunt_session
SESSION_DAYS=30

# Dev-only sign-in (POST /api/auth/dev/login). Disable in production.
AUTH_DEV_MODE=true

# Force Secure cookies (optional; auto-enabled when FRONTEND_URL / WEB_ORIGIN use https://)
# COOKIE_SECURE=true

# --- Railway (set in the Railway dashboard, not in .env) ---
# Backend service (repo root, config: /deploy/railway.backend.toml):
#   DATABASE_URL=${{Postgres.DATABASE_URL}}
#   WEB_ORIGIN=https://${{Frontend.RAILWAY_PUBLIC_DOMAIN}}
#   FRONTEND_URL=https://${{Frontend.RAILWAY_PUBLIC_DOMAIN}}
#   AUTH_DEV_MODE=false
#
# Frontend service (root: /app/webReact, config: /app/webReact/railway.toml):
#   BACKEND_UPSTREAM=${{Backend.RAILWAY_PRIVATE_DOMAIN}}:${{Backend.PORT}}
#
# See docs/railway.md for full setup steps.
