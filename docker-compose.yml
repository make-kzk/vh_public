services:
  db:
    image: postgres:18-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: vibehunt
      POSTGRES_PASSWORD: vibehunt
      POSTGRES_DB: vibehunt
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql

volumes:
  pgdata:
